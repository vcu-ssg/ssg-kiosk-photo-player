# Example environment configuration for the Photo Kiosk
# Copy this file to ~/.env and update with your real credentials.
# Then reference it from docker-compose.yml with an absolute path.

# --- App Settings ---
NODE_ENV=production
PORT=3000
CLIENT_ID=my-client-name

# --- API Keys ---
# Replace with your personal OpenWeather API key
OPENWEATHER_KEY=your_openweather_api_key_here
